default:
tags:
  - CICD

stages:
  - build

build-job:
stage: build
script:
  - echo "Run build"
  - cd src/cat
  - make
  - cd ../grep
  - make

artifacts:
paths:
  - src/cat
  - src/grep
expire_in: 30 days
